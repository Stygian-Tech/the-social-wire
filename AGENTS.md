## Learned User Preferences

- When asked to commit and push, stage only files changed for the current task, exclude unrelated edits, use a short conventional commit message, and push the active branch (often `main`).
- Prefer focused diffs: match existing project style, keep imports at the top, and avoid drive-by refactors or unsolicited docs.
- When a delegated or subagent outcome is already visible in the parent UI, do not restate its full contents unless asked or multi-task synthesis is needed; a brief confirmation is enough (and avoid repeating the same stock confirmation every time).
- When executing an attached implementation plan, do not edit the plan file; use existing to-dos, mark them `in_progress` while working, and avoid creating duplicate to-dos.
- Use Title Case for product UI options, menu actions, and button labels unless existing copy clearly follows another pattern.

## Learned Workspace Facts

- The Next.js app lives in `apps/web`; common checks are `bun run turbo run build --filter=web...` and targeted `bun test` under `apps/web`. Root metadata in `apps/web/src/app/layout.tsx` sets the default Open Graph/Twitter image to `/og/the-social-wire.png` with `metadataBase` from `NEXT_PUBLIC_SITE_URL`, else `VERCEL_URL` on Vercel, else `https://thesocialwire.app`. The signed-in “My Publications” list is at route `/me/publications` (profile-linked).
- The `/saved` read-later route uses the same three-pane shell as `/read` (list column, detail with `EntryArticleEmbed`; `saved/layout` keeps `main` overflow-hidden and `saved/settings` scrolls inside the page).
- LATR Link HTTPS read-later merges and persists via `com.latr.saved.*` records on the user’s PDS (lexicons in `packages/lexicons`). Do not add LATRHTTPS merge endpoints to **`services/api`** unless explicitly scoped—that flow stays elsewhere (often the web stack or another backend). **`services/api` gateway** work should target cross-platform OAuth, per-user settings sync, and PDS-oriented caching with minimal stored data—**not** an AppView for feeds or discovery; keep **L@tr.link** on its own service rather than folding it into this API.
- Feed entry read/unread uses browser `the-social-wire.read-state.v1` (see `entryReadStateStorage`) and, when signed in, syncs first-read timestamps to `com.thesocialwire.entryReadState` on the user’s PDS (deterministic rkey = base32(SHA-256(entry AT-URI)), same scheme as L@tr item keys). OAuth scopes in `auth.ts` + `public/client-metadata.json` must include `repo:com.thesocialwire.entryReadState?...`; widening requires sign-out/in.
- ATProto OAuth uses `atproto` scope: `useAuth().session` is a lightweight `{ did }` shape; anything that needs `fetchHandler` / OAuth on the wire must use `getOAuthSession()` (`OAuthSession`).
- **Local `next dev`:** parameterized loopback OAuth builds `http://localhost?redirect_uri=…` (with `127.0.0.1`/`[::1]` and `/callback`) when `NEXT_PUBLIC_APP_ENV` is `local` or unset in development; production uses discoverable client metadata (see `apps/web/src/lib/auth.ts` and README).
- OAuth access tokens here are typically audience-bound to the user’s PDS, not the Bluesky App View—use a plain `Agent('https://bsky.social')` without OAuth-backed `fetch` for public App View reads (e.g. follows, `app.bsky.actor.getProfile`), and use a session-backed `Agent` / `createOAuthAgent` for `com.atproto.repo.*` and other PDS-targeted calls; do not attach the OAuth token to arbitrary `bsky.social` XRPC. When bridging `session.fetchHandler` into `@atproto/api`’s `Agent`, satisfy `typeof fetch` (including static `preconnect`) by delegating `preconnect` from global `fetch` on the wrapper.
- **`apps/apple` native ATProto OAuth** uses PKCE and `ASWebAuthenticationSession`, with code/refresh via **`POST {pds}/oauth/token`**; **PAR and DPoP proofs are not implemented** in `ATProtoOAuthService` today (file header may still say DPoP—treat **`apps/apple/README.md`** as the accurate wire description). Native **`redirect_uri`** uses reversed **`client_id`** host labels with a single slash after the scheme (e.g. `app.thesocialwire:/oauth/callback`; see **`ATProtoOAuthConfig`**). Hosted metadata is **`apps/web/public/ios-client-metadata.json`**; **`ATProtoOAuthClientID`** in Info.plist selects an alternate HTTPS **`client_id`** for previews.
- standard.site discovery should cover `site.standard.document` and `site.standard.publication`, and retain `site.standard.entry` for backward compatibility; `services/api`’s Swift `DiscoveryChain.swift` reflects the intended multi-collection behavior. In the web app, sidebar subscribed sources merge `site.standard.graph.subscription` with Skyreader `app.skyreader.feed.subscription` records (`usePublicationSidebarData`, `pdsClient`). The Add Publication flow (`AddPublicationDialog` + `/api/resolve-add-publication`) prefers subscribing to a resolved standard.site publication when possible, otherwise falls back to RSS/Atom and writes a Skyreader feed subscription; RSS branding resolves feed icons from `channel.image.url` / `itunes:image`, stores them as `customIconUrl`, and uses site/feed-origin favicon fallbacks for sidebar avatars.
- GitHub Wiki markdown is authored under **`docs/wiki/`** and synced to the repo’s **`.wiki.git`** by **`.github/workflows/publish-wiki.yml`** on **`main`** (and **`workflow_dispatch`**) using **`rsync -av --delete --exclude='.git'`**—wiki-only files not mirrored in `docs/wiki/` are removed on sync. The committed env template is **`apps/web/.env.example`** (`apps/web/.gitignore` un-ignores it with **`!.env.example`**). The repository **`.gitignore`** includes **`.cursor/`** so local Cursor metadata is not committed.
- Bridgy bridged blobs (`atproto.brid.gy` → `com.atproto.sync.getBlob`) can `400`; `http://` variants bump mixed-content—prefer HTTPS, non-Bridgy fallbacks over a single Bridgy blob `<img>` source; strip Bridgy-ish query noise (`bridge_completed` and related `bridge_*`) when normalizing reader/embed URLs to avoid publisher `404`s (iframes/outbound navigation).
- In `apps/web`, `bun run dev` runs `next dev --webpack` by default (`bun run dev:turbo` uses Turbopack). Keep `turbopack.root` in `next.config.ts` at the monorepo root (`../..`) so `next build` resolves the workspace and Turbopack does not infer a lockfile under `$HOME`; setting the root to `apps/web` alone breaks production builds. Next allows only one `next dev` per app directory—a stuck `next-server` or stale `apps/web/.next/dev/lock` can block startup; stop the old process (e.g. `kill -9` the listening PID) before deleting the lock if needed. For Xcode/Simulator CLI, **`destinations` like `platform=iOS Simulator,name=iPhone 16` can match multiple OS versions—prefer an explicit Simulator `id` or `generic/platform=iOS`** for repeatable builds/tests. **CI:** `.github/workflows/ci.yml` path-gates **`build-web`** and **`test-api`** (`dorny/paths-filter`); **`test-api`** uses **`swift-actions/setup-swift@v3`** (Swiftly) with Swift **6.2+** and **`skip-verify-signature: true`** where needed for Ubuntu runner GPG verify quirks. **Swift API:** in **`services/api`**, **`App`** and **`AppTests`** compile with **`-warnings-as-errors`** in **`Package.swift`** (warnings fail **`swift test`** and release/Docker builds). **Fly deploy:** **`.github/workflows/deploy.yml`** runs **`flyctl deploy ./services/api`** from the repo root so **`services/api/fly.toml`** is picked up. **Supabase:** CLI config and migrations live under root **`supabase/`**; **`.github/workflows/supabase.yml`** is path-gated on **`supabase/**`**, validates migrations on PRs, and on push runs **`supabase link`** + **`supabase db push`** (default pooler / IPv4-friendly on GitHub runners) to **`dev`** vs **`main`** with separate **`SUPABASE_DEV_*`** and **`SUPABASE_PROD_*`** secrets—**`SUPABASE_*_DB_PASSWORD`** must be the raw Postgres password for that project ref (not a full **`postgres://`** URI). **`.github/workflows/*.yml`** (CI, deploy, wiki, Supabase) set **`FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true`** while GitHub Actions migrates JavaScript actions off Node 20.
