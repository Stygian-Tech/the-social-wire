{
  "lexicon": 1,
  "id": "app.thesocialwire.wireFeedback",
  "defs": {
    "main": {
      "type": "record",
      "description": "A viewer-authored quality assessment of a public article shown on The Wire. The record is public and may be used only as an aggregate ranking signal.",
      "key": "any",
      "record": {
        "type": "object",
        "required": ["canonicalUrl", "value", "createdAt", "updatedAt"],
        "properties": {
          "canonicalUrl": {
            "type": "string",
            "format": "uri",
            "maxLength": 2048,
            "description": "The canonical public HTTP(S) article URL assessed by the viewer."
          },
          "subject": {
            "type": "string",
            "format": "at-uri",
            "maxLength": 2048,
            "description": "Optional public AT-URI representing the story when one is available."
          },
          "value": {
            "type": "string",
            "knownValues": ["good", "not_good"],
            "description": "Whether the viewer considered this a good article."
          },
          "createdAt": {
            "type": "string",
            "format": "datetime",
            "maxLength": 64
          },
          "updatedAt": {
            "type": "string",
            "format": "datetime",
            "maxLength": 64
          }
        }
      }
    }
  }
}
