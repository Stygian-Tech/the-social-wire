meta {
  name: Resolve Alert (XRPC)
  type: http
  seq: 124
}

post {
  url: {{baseUrl}}/xrpc/app.thesocialwire.operations.resolveAlert
  body: json
  auth: none
}

headers {
  Authorization: Bearer {{oauthAccessToken}}
  DPoP: {{dpopProof}}
  Idempotency-Key: {{operationsIdempotencyKey}}
  Content-Type: application/json
}

body:json {
  { "id": "{{operationsAlertId}}", "expectedVersion": {{operationsAlertVersion}}, "idempotencyKey": "{{operationsIdempotencyKey}}", "auditNote": "Resolve after review", "environmentConfirmation": "{{operationsEnvironmentConfirmation}}" }
}
