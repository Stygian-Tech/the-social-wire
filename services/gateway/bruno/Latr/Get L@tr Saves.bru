meta {
  name: Get L@tr Saves
  type: http
  seq: 10
}

get {
  url: {{baseUrl}}/v1/latr/saves
  body: none
  auth: none
}

headers {
  Authorization: DPoP {{oauthAccessToken}}
  DPoP: {{dpopProof}}
  X-Latr-Gateway-DPoP: {{latrGatewayDpopProof}}
  X-ATProto-Upstream-DPoP: {{upstreamDpopProof}}
}

docs {
  Proxies to the external L@tr Gateway for native iOS clients. Requires `LATR_IOS_PROXY_*` variables on the Social Wire Gateway service (distinct from the web frontend's `LATR_GATEWAY_*`).
}
