# Standard Site fixtures

These fixtures preserve the field shape observed in public `site.standard.publication`
and `site.standard.document` records on 2026-08-20. DIDs, CIDs, URLs, titles, text,
and timestamps are synthetic so tests never depend on or redistribute publisher data.
