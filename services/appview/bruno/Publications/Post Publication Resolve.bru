meta {
  name: Post Publication Resolve
  type: http
  seq: 12
}

post {
  url: {{baseUrl}}/xrpc/app.thesocialwire.publication.resolvePublication
  body: json
  auth: none
}

headers {
  Authorization: Bearer {{oauthAccessToken}}
  DPoP: {{dpopProof}}
  Content-Type: application/json
}

body:json {
  {
    "input": "{{resolveInput}}"
  }
}
