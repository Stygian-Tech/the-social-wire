meta {
  name: Post AppView Read Mark
  type: http
  seq: 23
}

post {
  url: {{baseUrl}}/v1/appview/read-marks
  body: json
  auth: none
}

headers {
  Authorization: Bearer {{oauthAccessToken}}
  DPoP: {{dpopProof}}
  Content-Type: application/json
}

body:json {
  {
    "subjectUri": "{{entrySubjectUri}}",
    "readAt": "{{readMarkAt}}"
  }
}
