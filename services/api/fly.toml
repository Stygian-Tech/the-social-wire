# fly.toml app configuration file generated for social-wire-api on 2026-05-18T01:27:43Z
#
# Deploy from this directory (monorepo root = Docker build context):
#   fly deploy ../.. --config services/api/fly.toml --app the-social-wire-dev
# Or: bash deploy.sh dev

app = 'the-social-wire-dev'
primary_region = 'ams'

[build]
  dockerfile = 'Dockerfile'

[deploy]
  wait_timeout = '10m0s'

[env]
  PORT = '8080'
  # Mounts /v1/appview/* (requires SUPABASE_DATABASE_URL secret + worker ingest).
  ENABLE_THIN_APPVIEW = 'true'
  # `/ios-client-metadata.json` derives `redirect_uris` from the request Host (`client_id` origin), reversed —
  # no separate redirect override. `OAUTH_PUBLIC_ORIGIN` applies only to `/oauth/client-metadata.json`.

[http_service]
  internal_port = 8080
  force_https = true
  auto_stop_machines = 'stop'
  auto_start_machines = true
  min_machines_running = 0

  [[http_service.checks]]
    interval = '30s'
    timeout = '10s'
    grace_period = '1m0s'
    method = 'GET'
    path = '/health'

[[vm]]
  memory = '1gb'
  cpu_kind = 'shared'
  cpus = 1
  memory_mb = 256
