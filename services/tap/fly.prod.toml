app = 'the-social-wire-prod-tap'
primary_region = 'ams'

[build]
  dockerfile = 'Dockerfile'
  context = '../..'

[deploy]
  wait_timeout = '10m0s'

[env]
  TAP_ENV = 'prod'
  TAP_BIND = ':2480'
  TAP_COLLECTION_FILTERS = 'site.standard.document,site.standard.entry'
  TAP_DISABLE_ACKS = 'false'
  TAP_FULL_NETWORK = 'false'
  TAP_NO_REPLAY = 'false'
  TAP_LOG_LEVEL = 'info'
  # Indigo Tap defaults to 32 connections; keep headroom in the shared session pool.
  TAP_MAX_DB_CONNS = '1'
  # Repo CARs are held in memory during resync; serialize them to bound peak memory.
  TAP_RESYNC_PARALLELISM = '1'
  TAP_METRICS_LISTEN = ':2481'
  TAP_RELAY_URL = 'https://relay1.us-east.bsky.network'

[checks.health]
  type = 'tcp'
  port = 2480
  interval = '15s'
  timeout = '5s'
  grace_period = '30s'

[[vm]]
  memory = '2gb'
  cpu_kind = 'shared'
  cpus = 1
